profiles {
    docker {
        docker.enabled = true
    }
    podman {
        podman.enabled = true
        podman.runOptions = '--user $(id -u):$(id -g) --userns=keep-id --security-opt label=disable'
    }
}
